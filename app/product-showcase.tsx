import {ArrowUpRight} from 'lucide-react';
import {productCollection} from './products-data';
import ProductStage from './product-stage';
import './product-design.css';
export default function ProductShowcase(){return <section className="collection-section" id="products"><div className="wrap"><div className="collection-heading"><div><p className="eyebrow">THE WASSERMECH COLLECTION</p><h2>Exceptional spaces.<br/><em>Start beneath the surface.</em></h2></div><p>From quiet drainage to the finishing detail.<br/>Find the right system for every connection.</p></div><div className="collection-grid">{productCollection.map((p,i)=><article className={'collection-card collection-'+p.slug+(i===0?' collection-featured':'')} key={p.slug}><a href={'/products/'+p.slug} aria-label={'Explore '+p.name+' — '+p.category}><ProductStage className={'stage-'+p.slug}><span className="stage-index">{p.number} / 07</span><span className="stage-category">{p.tag}</span><div className="stage-picture"><picture>{p.slug==='coestilen'&&<source srcSet="/assets/crops/coestilen-fit.png"/>}<img src={'/assets/'+p.image} alt={p.imageAlt} loading="lazy"/></picture></div><span className="stage-caption">{p.category}</span></ProductStage><div className="collection-copy"><span className="eyebrow">{p.tag}</span><h3>{p.name}</h3><p>{p.category}</p>{i===0&&<p className="collection-summary">Three complementary systems. From low-noise internal drainage to outdoor sewer networks.</p>}<span className="collection-link">Explore the range <ArrowUpRight size={20}/></span></div></a></article>)}</div></div></section>}
